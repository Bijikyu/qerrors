'use strict';

const localVars = require('../../config/localVars');
const { HTTP_STATUS, DEFAULT_MESSAGES, LOG_LEVELS } = localVars;

module.exports = {
  HTTP_STATUS,
  DEFAULT_MESSAGES,
  LOG_LEVELS
};